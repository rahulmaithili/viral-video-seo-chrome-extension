# Example environment configuration for Viral Video AI Studio
# Note: In Chrome Extension mode, keys are configured inside the Settings UI and stored securely in chrome.storage.local.

VITE_GEMINI_API_KEY=
VITE_DEFAULT_MODEL=gemini-3.6-flash
VITE_ENABLE_MOCK_BY_DEFAULT=true
